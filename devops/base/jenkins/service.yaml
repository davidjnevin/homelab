apiVersion: v1
kind: Service
metadata:
  name: jenkins-service
  namespace: devops-tools
spec:
  selector:
    app: jenkins-server
  type: NodePort
  ports:
    - name: jnlpport
      port: 32000
      targetPort: 32000
      nodePort: 32000
